#!/bin/sh

STORAGE="./dots"

lscopy() {
    for item in ${1}
    do
        srce="${2}/${item}"
        dest="${3}/${item}"
        path="${3}/$(dirname $item)"

        if [ -e ${srce} ]; then
            if [ -e ${dest} ]; then
                DIFF=$((`date -r "${srce}" '+%s'` - `date -r "${dest}" '+%s'`))

                if [ $DIFF -eq 0 ]; then
                    echo -e " -  path ${srce} not modified"
                    cp -xarf "${srce}" "${path}"
                else
                    cp -xarf "${srce}" "${path}" && echo -e "\033[33m -  path ${srce} was modified\033[0m"
                fi
            else
                mkdir -p "${path}" > /dev/null 2>&1
                cp -xarf "${srce}" "${path}" && echo -e "\033[32m -  copy: ${srce} to ${path}\033[0m"
            fi
        else
            echo -e "\033[31m -  path ${srce} not exists\033[0m"
        fi
    done
}


case $1 in
packages)
    case $2 in
    install)
        source inc/packages
        echo -e "\033[32m-- Install packages from list --\033[0m"

        echo -e "Fonts:\n    ${FONTS[*]}"
        echo -e "Themes:\n    ${THEMES[*]}"
        echo -e "Utils:\n    ${UTILS[*]}"
        echo -e "Media:\n    ${MEDIA[*]}"
        echo -e "WM:\n    ${WM[*]}"

        pamac install --as-deps ${FONTS[*]} ${THEMES[*]} ${UTILS[*]} ${MEDIA[*]} ${WM[*]}
        ;;

    purge)
        source inc/rmpackages
        echo -e "\033[33m-- Remove packages from list --\033[0m"

        if [ ! ${PURGE_PKGS[*]} ]; then
            echo -e "Not packages in remove list"
            exit 0
        fi

        echo -e "To remove:\n    ${PURGE_PKGS[*]}"
        pamac remove ${PURGE_PKGS[*]}
        ;;

    *)
        echo -e "Usage: manage packages {install|purge}"
        ;;

    esac
    ;;

backup)
    source inc/configs
    echo -e "-- Copy configs to ${STORAGE} repository --"

    LIST="${MAIN[*]}"

    if [[ $2 = "all" ]]; then
        LIST="${MAIN[*]} ${EXTRA[*]}"
    fi

    mkdir -p "${STORAGE}" > /dev/null 2>&1
    lscopy "${LIST}" "${HOME}" "${STORAGE}"

    [ -d .git ] && git status
    ;;

restore)
    source inc/configs
    echo -e "-- Copy configs from '${STORAGE}' repository --"

    LIST="${MAIN[*]}"

    if [[ $2 = "all" ]]; then
        LIST="${MAIN[*]} ${EXTRA[*]}"
    fi

    lscopy "${LIST}" "${STORAGE}" "${HOME}"
    ;;

purge)
    echo -e "\033[31m-- Purge ${STORAGE} directory --\033[0m"
    rm -rf ${STORAGE}
    ;;

*)
    echo -e "Usage: manage {packages|backup|restore|purge} [{install|purge}] [all]"
    echo -e "\tpackages - install and remove all packages from list"
    echo -e "\tbackup   - backup user configs to '${STORAGE}' repository"
    echo -e "\trestore  - restore user configs from '${STORAGE}' repository"
    echo -e "\tpurge    - clear repository configs"
    ;;
esac


exit 0
